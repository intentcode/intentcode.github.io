services:
  landing:
    build: .
    ports:
      - "8080:80"
    volumes:
      # Mount files for live reload during development
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./style.css:/usr/share/nginx/html/style.css:ro
      - ./script.js:/usr/share/nginx/html/script.js:ro
